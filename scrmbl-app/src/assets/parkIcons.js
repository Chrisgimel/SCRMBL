// Real park badge art goes here once it exists — keyed by the exact string
// used in a hike's `park` field (constants/index.js). Until an entry is
// added, ParkStamp.js draws a generic placeholder stamp instead, so the
// passport works end-to-end with zero art and upgrades one park at a time.
//
// Add real art the same way images.js does — import the file and reference
// it here, e.g.:
//   import rmnp from "./parks/rmnp.png";
//   export const PARK_ICONS = { "Rocky Mountain National Park": rmnp };
export const PARK_ICONS = {};
